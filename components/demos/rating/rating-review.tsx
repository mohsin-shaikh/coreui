import { StarRating } from '@/components/ui/svg-rating-icons';

export default function RatingReview() {
  return <StarRating rating={4.5} />;
}

export const code = `import { StarRating } from '@/components/ui/svg-rating-icons';

export default function RatingReview() {
  return <StarRating rating={4.5} />;
}`;
