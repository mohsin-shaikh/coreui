import * as Divider from '@/components/ui/divider';

export default function DividerLine() {
  return <Divider.Root variant="line" />;
}

export const code = `import * as Divider from '@/components/ui/divider';

export default function DividerLine() {
  return <Divider.Root variant="line" />;
}`;
