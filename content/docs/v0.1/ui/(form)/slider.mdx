---
title: Slider
description: Allowing precise selection of a single value within a specified range.
---

<ComponentPreview name="slider-demo" className="w-full max-w-none" />

## Installation

<Steps>
  <Step>
    ### Install the following dependencies:

    <Tabs items={['npm', 'pnpm', 'yarn']}>
      <Tab value="npm">
        ```bash
        npm install @radix-ui/react-slider
        ```
      </Tab>
      <Tab value="pnpm">
        ```bash
        pnpm add @radix-ui/react-slider
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
        yarn add @radix-ui/react-slider
        ```
      </Tab>
    </Tabs>
  </Step>
  <Step>
    ### Create a `slider.tsx` file and paste the following code into it.

    <ComponentSource src="components/ui/slider.tsx" title="components/ui/slider.tsx" />
  </Step>
  <Step>
    ### Check the import paths to ensure they match your project setup.
  </Step>
</Steps>

## Examples

### Range

<ComponentPreview name="slider-range" className="w-full max-w-none" />

### With Tooltip

<ComponentPreview name="slider-with-tooltip" className="w-full max-w-none" />

## API Reference

This component is based on the Radix UI Slider primitives. Refer to their [documentation](https://www.radix-ui.com/primitives/docs/components/slider) for the API reference.
