---
title: Avatar Group
description: Avatar group displays two or more avatars in an inline stack.
---

<ComponentPreview name="avatar-group-demo" />

## Installation

<Steps>
  <Step>
    ### This component is intended to be used with the Avatar component. Refer to the [Avatar](/docs/v0.1/ui/avatar) documentation for setup instructions before using this component.
  </Step>
  <Step>
    ### Create an `avatar-group.tsx` file and paste the following code into it.

    <ComponentSource src="components/ui/avatar-group.tsx" title="components/ui/avatar-group.tsx" />
  </Step>
  <Step>
    ### Check the import paths to ensure they match your project setup.
  </Step>
</Steps>

## Examples

### Size

<ComponentPreview name="avatar-group-size" />

## API Reference

### AvatarGroup.Root

This component is based on the `<div>` element and supports all of its props and adds:

| Prop | Type | Default |
| --- | --- | --- |
| `size` | `"80"` \| `"72"` \| `"64"` \| `"56"` \| `"48"` \| `"40"` \| `"32"` \| `"24"` \| `"20"` | `"80"` |

### AvatarGroup.Overflow

Displays an overflow indicator when the number of avatars exceeds the visible space. Typically shows a count of extra avatars not displayed. This component is based on the `<div>` element and supports all of its props.
